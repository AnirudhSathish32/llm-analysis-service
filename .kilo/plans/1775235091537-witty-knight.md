# Plan: Merge critical branch into main

## Current State

- Branch: `critical` — 4 commits ahead of main, diverged
- Main now contains: PR #1 (fix-medium-issues) + PR #2 (fix-anti-patterns)
- Critical branch has 3 slices, all with `status: pending` but code already committed
- Rebase onto main produces conflicts in `.env`, `.env.example`, `app/core/config.py`, `docker-compose.yaml`

## Problem

The critical branch slices show `pending` but the code changes are already committed. The slices were written but never executed via slice-builder — the user implemented changes directly. The branch also has significant merge conflicts with main because both branches modified the same files (config.py, docker-compose.yaml, .env).

## Steps

### 1. Mark slices as completed
- All 3 slice files in `.kilo/slices/critical-fixes/` need `status: pending` → `status: completed`
- Reason: Code changes are already committed in the 4 commits on this branch

### 2. Rebase onto latest main
- `git fetch origin main`
- `git rebase origin/main`
- Conflicts expected in:
  - `.env` — critical has dev_user:dev_password, main has ci_user:ci_password → keep main's version
  - `.env.example` — both added it → keep main's more complete version
  - `app/core/config.py` — critical removed ChromaDB + added validator, main removed ChromaDB + added pooling → merge both
  - `docker-compose.yaml` — critical removed ChromaDB, main already removed it → keep main's version
- Resolution strategy: For overlapping changes, prefer main's version (already has the same fixes from anti-patterns branch)

### 3. Run tests after rebase
- `docker compose build api`
- `docker compose run --rm api pytest tests/ -v --tb=short`
- Expected: All tests pass (main has 57 passing, critical adds test_hardcoded_creds.py, test_upload_security.py, test_error_sanitization.py)

### 4. Create PR
- Title: `fix(security): critical security fixes — credentials, upload, error sanitization`
- Base: main
- Body: Auto-generated from slice files
- Reviewer: solo (per project config)

## Risks

- The critical branch's changes overlap significantly with what's already on main (both fixed hardcoded creds, ChromaDB removal). The unique additions from critical are: file size limit, path traversal protection, error sanitization, and the credential validator.
- After rebase, the diff will be smaller since main already has many of the same fixes.
