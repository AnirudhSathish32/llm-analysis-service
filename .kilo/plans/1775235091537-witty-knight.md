# Plan: Fix Remaining Codebase Issues

## Recon Summary

40 issues found across the codebase: 15 critical, 17 medium, 8 low.
Already fixed in previous branches: print() removal, exception handling, config cleanup, ChromaDB removal, dead code deletion, API keys in Settings, flake8 F401/F821 removal, type hints on session/rate_limiter.

## Remaining Issues by Priority

### CRITICAL (11 remaining)
1. Duplicate client initialization in `llm_router.py` (lines 29-41)
2. RAG embedding calls block event loop (`rag_pipeline.py` lines 38-53)
3. Pinecone upsert blocks event loop (`rag_pipeline.py` line 135)
4. File upload reads entire file into memory (`documents.py` lines 55-68)
5. `llm_router.py` initializes clients with empty string API keys (lines 29, 31)
6. `rag_pipeline.py` uses `os.getenv()` bypassing Settings (lines 14-17)
7. `TimeoutError` shadows Python built-in (`exceptions.py` line 21)
8. No authentication on any endpoint
9. Rate limiter only on analysis endpoint
10. `documents.py` reads `UPLOAD_DIR` via `os.getenv()` (line 17)
11. `create_all_tables` used instead of Alembic (`main.py` line 21)

### MEDIUM (17 remaining)
12. Duplicate error-handling boilerplate in `analysis_service.py`
13. `llm_router.py` catches `Exception` for provider fallback
14. `metrics.py` catches `Exception` instead of `SQLAlchemyError`
15. `analysis_service.py` catches `Exception` in RAG block
16. `documents.py` imports `select` inside function (line 110)
17. Duplicate test method name in `test_logging.py` (lines 130, 155)
18. `documents.py` doesn't validate `document_id` UUID (line 112)
19. Rate limiter uses `client.host` — LB IP in production
20. No response models on documents/metrics endpoints
21. Duplicate "Running Locally" section in README
22. `CACHE_TTL_SECONDS` default wrong in README
23. `StoredAnalysisSchema` defined but never used
24. `app/api/dependencies.py` is empty
25. `google-generativeai`, `langchain` outdated
26. CD rollback condition flawed
27. CI installs flake8 separately

### LOW (8 remaining)
28. No module-level docstrings (`llm_router.py`, `rag_pipeline.py`)
29. `health` endpoint missing docstring/type hints
30. `get_session` missing return type hint
31. `test_db_pooling.py` accesses private attributes
32. No `app/__init__.py`
33. No `pyproject.toml`
34. No code formatter (black/ruff)
35. No mypy in CI

## Proposed Tasks (Grouped by Theme)

### Task 1: `fix-blocking-calls` (Performance — Medium Severity)
**Goal:** Make all synchronous calls non-blocking to prevent event loop starvation.
- Remove duplicate client initialization in `llm_router.py`
- Wrap RAG `genai.embed_content()` in `asyncio.to_thread()`
- Wrap Pinecone `index.upsert()` in `asyncio.to_thread()`
- Stream file upload instead of `await file.read()`
**Slices:** 4 | **Tests:** ~16

### Task 2: `fix-config-consistency` (Config — Low Severity)
**Goal:** Centralize all configuration through pydantic-settings.
- Move `rag_pipeline.py` config to `Settings` class
- Add `upload_dir` to `Settings`
- Remove stale `CHROMA_HOST`/`CHROMA_PORT` from `task-definition.json`
- Rename `TimeoutError` to `LLMTimeoutError` to fix shadowing
**Slices:** 4 | **Tests:** ~8

### Task 3: `fix-code-quality` (Code Quality — Low Severity)
**Goal:** Clean up dead code, duplicate tests, and missing type hints.
- Remove duplicate test in `test_logging.py`
- Move `select` import to module level in `documents.py`
- Delete `StoredAnalysisSchema` and unused `datetime` import
- Delete empty `app/api/dependencies.py`
- Remove redundant imports in `test_error_sanitization.py`
- Add type hints to `get_session`, `rate_limiter`, `health`
- Add docstring to `health` endpoint
**Slices:** 3 | **Tests:** ~6

### Task 4: `fix-documentation` (Documentation — Low Severity)
**Goal:** Fix README inconsistencies and add missing docstrings.
- Remove duplicate "Running Locally" section
- Fix `CACHE_TTL_SECONDS` default in README
- Remove live API IP from README
- Add module docstrings to `llm_router.py`, `rag_pipeline.py`
**Slices:** 2 | **Tests:** ~4

### Task 5: `add-missing-tests` (Testing — High Severity)
**Goal:** Add test coverage for all untested endpoints and services.
- Tests for `documents.py` `get_document` endpoint
- Tests for `metrics.py` endpoints
- Tests for `health` endpoint
- Tests for `rag_pipeline.py` (ingest, retrieve, empty docs)
- Tests for `rate_limit.py`
- Test for LLM timeout path
- Test for cache miss where Redis `set` fails
**Slices:** 4 | **Tests:** ~24

### Task 6: `fix-ci-cd` (CI/CD — Medium Severity)
**Goal:** Fix CI/CD pipeline issues and add security scanning.
- Add CI gate to CD pipeline
- Fix CD rollback condition
- Add flake8 to `requirements-dev.txt`
- Add `pip-audit` security scanning
- Add Docker image vulnerability scan (trivy)
**Slices:** 3 | **Tests:** N/A (CI changes)

### Task 7: `fix-rate-limiting` (Security — High Severity)
**Goal:** Apply rate limiting to all endpoints and fix IP detection.
- Add rate limiting to documents and metrics endpoints
- Fix rate limiter to use `X-Forwarded-For` header
- Add UUID validation for `document_id` parameter
**Slices:** 3 | **Tests:** ~12

## Recommended Execution Order

1. **`fix-code-quality`** — Quick wins, low risk, immediate cleanup
2. **`fix-documentation`** — Quick wins, no behavior changes
3. **`fix-config-consistency`** — Config cleanup, enables better testing
4. **`fix-blocking-calls`** — Performance improvements, medium risk
5. **`add-missing-tests`** — Test coverage, high value
6. **`fix-rate-limiting`** — Security improvements
7. **`fix-ci-cd`** — Pipeline improvements

## Risks and Unknowns

- **Authentication** is a large task not included above — would need its own dedicated task
- **Alembic migration** requires significant refactoring — flagged as "requires approval" in project rules
- **RAG pipeline tests** require mocking Pinecone and Google Embeddings — complex test setup
- **Rate limiter IP fix** may break local development if not handled carefully
- **CD pipeline changes** affect production deployment — need careful testing

## Questions for Developer

1. Which task would you like to tackle first?
2. Should we include authentication as a separate task?
3. Is the Alembic migration worth tackling now, or defer?
4. For the rate limiter IP fix, should we support both local dev and production (X-Forwarded-For fallback)?
